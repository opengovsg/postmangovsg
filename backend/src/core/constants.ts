export enum ChannelType {
  SMS = 'sms',
  Email = 'email'
}

/**
 * @swagger
 *  components:
 *    schemas:
 *      ChannelType:
 *        type: string
 *        enum:
 *        - sms
 *        - email
 */