export interface EmailContent {
  subject: string;
  body: string;
}
/**
 * @swagger
 *  components:
 *    schemas:
 *      EmailContent:
 *        type: object
 *        properties:
 *          subject:
 *            type: string
 *          body:
 *            type: string
 */