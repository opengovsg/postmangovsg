import axios from 'axios'

function getOtpWithEmail(email: string) {
  return Promise.resolve()
}

function loginWithOtp(email: string, otp: string) {
  return Promise.resolve()
}

export {
  getOtpWithEmail,
  loginWithOtp,
}