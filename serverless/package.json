{
  "name": "serverless",
  "version": "1.0.0",
  "description": "See [verify-backup-docs](verify-backup/README.md) for details on `verify-backup`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --exec serverless offline start -s dev",
    "deploy": "npm run env && serverless deploy --stage ${ENV:-'staging'} --conceal"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.108",
    "nodemon": "^2.0.20",
    "serverless": "^3.23.0",
    "serverless-offline": "^11.1.3",
    "serverless-offline-lambda": "^1.0.6",
    "serverless-plugin-typescript": "^2.1.4"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7"
  }
}
